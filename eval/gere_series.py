from data_series import liste_series
from obj_serie import SerieTV
def affiche_series(liste_ser):
    for i in range(len(liste_ser)):
        print(liste_series[i])

def compte_serie_par_type(liste, type):
    nb_type=0
    for serie in liste:





affiche_series(liste_series)